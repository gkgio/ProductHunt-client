<resources>


    <style name="ProductHuntTheme.Base" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="android:windowNoTitle">true</item>
        <item name="windowActionBar">false</item>
    </style>

    <style name="AppTheme" parent="ProductHuntTheme.Base">
    </style>

    <style name="noWindowTranslucent" parent="AppTheme">
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <style name="MyCustomToolBarTheme" parent="AppTheme">
        <item name="android:textColorPrimary">@color/colorToolbarPrimaryText</item>
        <item name="android:color">@color/colorToolbarPrimaryText</item>
    </style>

    <style name="ThemeSpinner">
        <!-- Color when pressed -->
        <item name="colorAccent">#ffffff</item>
        <!-- Default color for the dropdown arrow and line -->
        <item name="colorControlNormal">#ffffff</item>
        <item name="android:popupBackground">#DFFFFFFF</item>
    </style>

</resources>
